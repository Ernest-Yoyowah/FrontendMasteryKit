// example 1 - sorting strings
const names = ["Ernest", "Nii", "Yoyowah", "Mike", "John", "Dan"];

// reverses the array
names.reverse();
console.log(names);

//example 2 - sorting numbers in reverse

const scores = [10, 20, 60, 40, 50, 80, 25, 42];

// automatically sorts from last to first
scores.reverse();
// console.log(scores);
